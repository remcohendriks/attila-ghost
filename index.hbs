{{!< default}}
<header class="blog-header {{#if @site.cover_image}} has-cover{{/if}}">
	<div class="inner">
		<div class="box blog-box">
			<img src="{{asset "images/remco.png"}}" alt="{{@site.title}}" class="blog-avatar" />
			<h1 class="blog-name">{{@site.title}}</h1>
			{{#if @site.description}}
			<span class="blog-description">{{@site.description}}</span>
			{{/if}}
			<div class="blog-tagline">
				<p>I am a programmer and like to build apps. Mostly related to Artificial Intelligence and Machine Learning.</p>
				<p>I published a <a href="https://arxiv.org/abs/1412.2432" target="_blank" rel="noopener noreferrer">paper</a> how to use plain web browsers to do distributed Machine Learning. It was <a href="https://neurips.cc/Conferences/2014/Schedule?showParentSession=4820" target="_blank" rel="noopener noreferrer">presented</a> at NeurIPS in 2014.</p>
				<p>I speak fluent Mandarin Chinese, which I studied at Tsinghua University in Beijing.</p>
			</div>
		</div>
		{{#if @site.cover_image}}
		<div class="blog-cover cover">
			<img
			srcset="{{img_url @site.cover_image size="s"}} 320w,
              {{img_url @site.cover_image size="m"}} 640w,
              {{img_url @site.cover_image size="l"}} 960w,
              {{img_url @site.cover_image size="xl"}} 1920w"
      src="{{img_url @site.cover_image size="xl"}}"
			alt="{{@site.title}}" />
		</div>
		{{/if}}
	</div>
</header>

<div id="index" class="container">

	<main class="content" role="main">

		{{> "loop"}}

	</main>

</div>
